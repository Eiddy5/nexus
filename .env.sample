# server 配置
HOST=0.0.0.0
PORT=8888

# nexus 配置
DEBOUNCE=3
MAX_DEBOUNCE=5

# moka 配置
MOKA_CAPACITY=1000000
MOKA_TIME_TO_IDLE=3600

# PostgreSQL 数据库配置（可选）
# 如果配置了 DATABASE_URL，将自动启用 PostgreSQL 扩展
# DATABASE_URL=postgres://username:password@localhost:5432/nexus

LOG_LEVEL=debug